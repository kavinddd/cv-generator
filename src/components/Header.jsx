import "../styles/header.css"
export default function Header() {
    const headerText = "CV Generator";
    return (
        <div className="header">
            <h1>{headerText}</h1>
        </div>

    )
}